APP_PORT=8080
MYSQL_ROOT_PASSWORD=root
JAVA_OPTS=-Xms256m -Xmx512m

# Leave empty when Docker Hub is reachable.
# If Docker Hub times out in WSL, set for example:
# DOCKERHUB_PREFIX=docker.1ms.run/library/
DOCKERHUB_PREFIX=
